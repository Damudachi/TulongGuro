# System Directive: Final Polish & Advanced UX Upgrades

I have reviewed your implementation plan. Your logic for scoping `localStorage` to `user.id` and using "Few-Shot Prompting" for section context is approved. However, DO NOT hide the Batch Upload feature—that is our core mechanic and must remain highly visible. 

Please read this master directive and execute the following upgraded plan:

## 1. Bug Fixes & UI Polish
*   **Walkthrough State (`Dashboard.jsx`):** Scope the onboarding dismissal to `hasSeenTeacherWalkthrough_${user.id}` so the banner correctly appears for brand new accounts created on the same browser.
*   **Unified Combobox (`BatchUpload.jsx`):** Merge the separate text search and dropdown into a single, modern Combobox component. Typing should filter a list directly below the input, and clicking selects the student.
*   **Lock HITL Workspace (`HITLWorkspace.jsx`):** The entire grading panel—including rubric sliders, strengths, areas for growth, and actionable steps—must be locked/disabled by default. Add a prominent "Edit Assessment" button next to the total score that unlocks these fields for teacher editing.

## 2. Advanced AI Context & Rubric Engine
*   **DepEd Standard Context (`server.js`):** Update the system prompt. Pass the exact `gradeLevel` and `subject` into the prompt. Explicitly command Gemini to act as a localized tutor and evaluate the essay strictly against Philippine DepEd K-12 MATATAG standards for that specific grade level.
*   **Few-Shot Section Memory (`server.js`):** Before calling Gemini to grade, fetch 3 recent, teacher-approved submissions from the *same section* and *same activity*. Inject these into the prompt as examples of the section's baseline and the teacher's preferred grading tone to establish context without bloating the conversation history.
*   **Banded Rubrics (`RubricManager.jsx`):** Update the `DEPED_RUBRICS` constant and AI prompt to support detailed "Scoring Bands." Instead of just descriptions, criteria must have ranges (e.g., "9-10: Perfect grammar, 6-8: Average with minor errors") to give the AI and the teacher highly specific baselines.

## 3. High-Impact Onboarding Features (Workflow Magic)
*   **OCR Name Extraction (Smart Combobox):** Instruct the Gemini Vision model to attempt to read the student's handwritten name on the uploaded paper. Pass this extracted name string back to the frontend so the Combobox can auto-fill or highlight the correct student, saving the teacher from typing it manually.
*   **The "Time-Saved" Celebration (Gamification):** On the very first successful "Validate & Release" action for a new teacher account, trigger a `react-confetti` animation and a success toast celebrating the time they just saved (e.g., *"Great job! You just saved ~5 minutes of manual grading."*).
*   **"Preview Student View" (Trust Building):** Add a button on the Teacher's Class Hub/Activity page that opens a modal showing a mockup of what the Grade 6 student will see (the Safe Space UI). This builds teacher trust in how the system handles AI feedback.

## Execution Command
Please acknowledge these upgrades. Begin by implementing the UI bug fixes from Section 1 (the `localStorage` fix, the Unified Combobox, and the Locked HITL State). Once those are stable, wait for my command to proceed to the backend AI Prompt upgrades in Section 2.