# System Directive: Curriculum Integration, Placeholder Purge, & Strict AI Tone

I have reviewed the initial plan. The schema additions (`ClassLesson` model) are structurally sound, but the plan missed the directive to delete analytics placeholders, and the current class creation endpoint will cause a severe HTTP Timeout crash due to LLM processing times. 

Please read this master directive and execute the following corrected plan:

## Phase 1: The Placeholder Purge (Execute This First)
*   **[MODIFY]** `src/pages/student/StudentDashboard.jsx` & Analytics components.
*   **Action:** Strip out ALL hardcoded/mock data arrays used for the Topic Mastery bars, Skill Trend charts, and upcoming deadlines. The UI must render strictly from the live API payload (`avgSkills`, `upcomingDeadlines`, `latestStrategy`). If the payload is empty, render a clean "No Data Available" empty state. We must verify the backend is truly driving the UI before proceeding.

## Phase 2: Schema & "Output Type" Sync
*   **[MODIFY]** `prisma/schema.prisma`
*   **Action:** 
    *   Add `curriculumFile` (String?) to `Class`. 
    *   Create the `ClassLesson` model with fields: `id`, `classId`, `title`, `description`, `defaultRubric` (JSON), and crucially, add `outputType` (String) so the system knows if the lesson requires an Essay, Survey, Outline, etc. 
    *   Add `classLessonId` (String?) to the `Activity` model.
    *   Run `npx prisma db push`.

## Phase 3: Anti-Timeout File Processing (Crucial)
*   **[MODIFY]** `server/server.js` 
*   **Action:** Do NOT parse the whole curriculum in the initial class creation endpoint. 
    *   *Step 1:* `POST /api/teacher/classes` accepts the curriculum file upload, saves the file URL to the database, creates the Class, and returns `200 OK` instantly so the UI doesn't freeze.
    *   *Step 2:* Create a separate `POST /api/teacher/classes/:id/parse-curriculum` endpoint. The frontend will call this immediately after creation and show a "Scanning Curriculum & Generating Rubrics..." loading screen. 
    *   *Step 3:* When calling Gemini to parse the uploaded curriculum, instruct it to return a structured JSON array. Each object in the array must contain: the week/topic `title`, the `description`, the inferred `outputType`, and the generated `defaultRubric`. Save these as `ClassLesson` records. *(Note: Rely on Gemini Vision's native PDF support. If a DOCX is uploaded, ensure you implement standard text extraction before passing to the prompt).*

## Phase 4: Dynamic Activity Builder
*   **[MODIFY]** `src/pages/teacher/ActivityBuilder.jsx` & `ClassHub.jsx`
*   **Action:** Add a "Select Lesson/Topic" dropdown populated by the class's parsed `ClassLesson` records. When a teacher selects a specific lesson, automatically update the Activity's `type` field to match the `ClassLesson.outputType` (e.g., auto-locking it to 'Essay' if the lesson dictates an essay).

## Phase 5: Strict, Objective AI Grading Tone
*   **[MODIFY]** `server/server.js` — AI Grading Prompt
*   **Action:** Update the grading prompt to strictly evaluate against the specific `ClassLesson.defaultRubric` attached to the activity. 
*   **Action:** Inject this strict persona command into the system prompt: *"You are an objective, strict academic evaluator. Write feedback ENTIRELY in English. Do NOT sugarcoat, do NOT use overly enthusiastic praise (e.g., 'Great job!', 'Awesome!'), and focus purely on clinical, constructive criticism based directly on the rubric criteria."* 
    *   *Exception:* If the subject explicitly contains "Filipino", override the English rule and write in Filipino, but maintain the strict, clinical, objective tone.

## Execution Command
Acknowledge these architectural corrections. Begin immediately with **Phase 1** (Deleting all placeholders in Analytics), then proceed sequentially to Phase 2 through Phase 5.